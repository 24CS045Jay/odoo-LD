import { z } from "zod";
export const tripSchema = z.object({ title: z.string().min(1).max(140), description: z.string().max(2000).optional(), destinations: z.array(z.object({ city: z.string().min(1), country: z.string().optional(), image: z.string().optional(), cityRef: z.string().optional() })).optional(), startDate: z.coerce.date().optional(), endDate: z.coerce.date().optional(), status: z.enum(["planning", "upcoming", "ongoing", "completed", "dreaming"]).optional(), budget: z.coerce.number().min(0).optional(), currency: z.string().max(8).optional(), isPublic: z.boolean().optional() });
export const sectionSchema = z.object({ city: z.string().min(1).max(120), cityRef: z.string().optional(), startDate: z.coerce.date().optional(), endDate: z.coerce.date().optional(), title: z.string().max(180).optional(), notes: z.string().max(2000).optional(), type: z.enum(["travel", "hotel", "activity", "sightseeing", "food", "transportation", "custom"]).optional(), budget: z.coerce.number().min(0).optional(), orderIndex: z.coerce.number().int().min(0).optional() });
export const itineraryActivitySchema = z.object({ catalogActivity: z.string().optional(), title: z.string().min(1).max(180), time: z.string().max(80).optional(), duration: z.string().max(80).optional(), cost: z.coerce.number().min(0).optional(), category: z.string().max(80).optional(), notes: z.string().max(2000).optional(), orderIndex: z.coerce.number().int().min(0).optional() });
export const expenseSchema = z.object({ tripSection: z.string().optional(), title: z.string().min(1).max(180), category: z.enum(["stay", "transport", "food", "activities", "other"]).optional(), amount: z.coerce.number().min(0), date: z.coerce.date().optional(), notes: z.string().max(1000).optional() });
