import { AnimatePresence, motion } from "framer-motion";
import { Filter, SlidersHorizontal } from "lucide-react";
import { useState } from "react";
export default function FilterBar({ label = "Find your route" }: { label?: string }) { const [open, setOpen] = useState<string | null>(null); const options = open === "group" ? ["Destination", "Travel style", "Month"] : open === "filter" ? ["Upcoming", "Planning", "Completed"] : ["Newest first", "Oldest first", "Budget"] ; const toggle = (name: string) => setOpen(current => current === name ? null : name); return <div className="flex flex-wrap items-center justify-between gap-3"><p className="text-[10px] font-black uppercase tracking-[0.18em] text-[var(--gold)]">{label}</p><div className="relative flex flex-wrap gap-2"><button onClick={() => toggle("group")} className="flex items-center gap-2 rounded-full border border-[var(--line)] bg-white px-4 py-2.5 text-xs font-bold text-[var(--navy)] transition hover:border-[var(--gold)]"><SlidersHorizontal size={14}/> Group by</button><button onClick={() => toggle("filter")} className="flex items-center gap-2 rounded-full border border-[var(--line)] bg-white px-4 py-2.5 text-xs font-bold text-[var(--navy)] transition hover:border-[var(--gold)]"><Filter size={14}/> Filter</button><button onClick={() => toggle("sort")} className="rounded-full border border-[var(--line)] bg-white px-4 py-2.5 text-xs font-bold text-[var(--navy)] transition hover:border-[var(--gold)]">Sort by</button><AnimatePresence>{open ? <motion.div initial={{ opacity: 0, scale: .95, y: -8 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: .95, y: -8 }} transition={{ duration: .15 }} className="absolute right-0 top-12 z-30 min-w-44 overflow-hidden rounded-2xl border border-[var(--line)] bg-white p-1.5 shadow-xl">{options.map(option => <button key={option} onClick={() => setOpen(null)} className="block w-full rounded-xl px-3 py-2 text-left text-xs font-bold text-[var(--navy)] hover:bg-[var(--sand)]">{option}</button>)}</motion.div> : null}</AnimatePresence></div></div>; }
