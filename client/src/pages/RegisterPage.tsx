// World Trotter visual style: welcoming account-start page that leads into the dashboard preview.
import { ArrowRight, Compass } from "lucide-react";
import { Link, useLocation } from "wouter";
import { useState } from "react";
import { ApiClientError, authApi, setAccessToken } from "@/api/client";
import BrandMark from "@/components/layout/BrandMark";
export default function RegisterPage() { const [, navigate] = useLocation(); const [form, setForm] = useState({ firstName: "", lastName: "", email: "", password: "" }); const [error, setError] = useState(""); const [pending, setPending] = useState(false); const update = (field: keyof typeof form) => (event: React.ChangeEvent<HTMLInputElement>) => setForm(current => ({ ...current, [field]: event.target.value })); const submit = async (event: React.FormEvent) => { event.preventDefault(); setPending(true); setError(""); try { const result = await authApi.register(form); setAccessToken(result.token); navigate("/dashboard"); } catch (cause) { setError(cause instanceof ApiClientError ? cause.message : "Unable to create your account"); } finally { setPending(false); } }; return <div className="grid min-h-screen place-items-center bg-[var(--sand)] px-5 py-10"><form onSubmit={submit} className="w-full max-w-xl rounded-[30px] border border-[var(--sand-dark)] bg-[var(--canvas)] p-7 shadow-xl shadow-[rgba(23,49,74,.08)] sm:p-10"><Link href="/" className="flex items-center justify-center gap-3"><BrandMark size={44}/><span className="font-serif text-2xl font-bold text-[var(--navy)]">World <span className="text-[var(--gold)]">Trotter</span></span></Link><div className="mt-9 text-center"><span className="mx-auto flex h-11 w-11 items-center justify-center rounded-2xl bg-white text-[var(--gold)] gold-ring"><Compass size={20}/></span><p className="mt-5 text-[10px] font-black uppercase tracking-[.2em] text-[var(--gold)]">Explore the World with Us</p><h1 className="mt-3 font-serif text-5xl font-bold leading-[.94] text-[var(--navy)]">Make room for more<span className="text-[var(--gold)]">.</span></h1></div><div className="mt-9 grid gap-4 sm:grid-cols-2"><input required value={form.firstName} onChange={update("firstName")} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3.5 text-sm outline-none focus:border-[var(--gold)]" placeholder="First name"/><input required value={form.lastName} onChange={update("lastName")} className="rounded-2xl border border-[var(--line)] bg-white px-4 py-3.5 text-sm outline-none focus:border-[var(--gold)]" placeholder="Last name"/><input required value={form.email} onChange={update("email")} type="email" className="sm:col-span-2 rounded-2xl border border-[var(--line)] bg-white px-4 py-3.5 text-sm outline-none focus:border-[var(--gold)]" placeholder="Email address"/><input required value={form.password} onChange={update("password")} type="password" minLength={8} className="sm:col-span-2 rounded-2xl border border-[var(--line)] bg-white px-4 py-3.5 text-sm outline-none focus:border-[var(--gold)]" placeholder="Create a password"/></div>{error && <p className="mt-4 text-sm font-semibold text-red-700">{error}</p>}<button disabled={pending} className="mt-6 flex w-full items-center justify-center gap-2 rounded-full bg-[var(--navy)] py-3.5 text-sm font-extrabold text-white disabled:opacity-60">{pending ? "Creating your account…" : <>Start the journey <ArrowRight size={16}/></>}</button><p className="mt-6 text-center text-sm text-[var(--ink-muted)]">Already have an account? <Link href="/login" className="font-extrabold text-[var(--navy)]">Log in</Link></p></form></div>; }
