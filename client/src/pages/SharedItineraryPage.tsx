import { ArrowRight, Copy, Globe2 } from "lucide-react";
import { useMutation, useQuery } from "@tanstack/react-query";
import { Link, useLocation, useRoute } from "wouter";
import { publicApi } from "@/api/client";
import AppShell from "@/components/layout/AppShell";
import ItineraryDayBlock from "@/components/itinerary/ItineraryDayBlock";
export default function SharedItineraryPage() { const [, params] = useRoute("/shared/:shareToken"); const [, navigate] = useLocation(); const token = params?.shareToken ?? ""; const query = useQuery({ queryKey: ["shared", token], queryFn: () => publicApi.getTrip(token), enabled: Boolean(token) }); const copy = useMutation({ mutationFn: () => publicApi.copyTrip(token), onSuccess: trip => { localStorage.setItem("world-trotter-active-trip", trip._id); navigate("/itinerary-builder"); } }); const trip = query.data?.trip; const days = query.data?.sections.map((section, index) => ({ day: `Day ${String(index + 1).padStart(2, "0")}`, date: section.startDate ? new Date(section.startDate).toLocaleDateString("en-GB", { day: "2-digit", month: "short" }) : "Date to choose", city: section.city, theme: section.title || "A day to explore", items: section.activities?.map(activity => activity.title) ?? ["No activities added yet"], cost: new Intl.NumberFormat("en-IE", { style: "currency", currency: trip?.currency ?? "EUR", maximumFractionDigits: 0 }).format(section.budget ?? 0) })) ?? []; return <AppShell><section className="mx-auto max-w-[1000px] px-5 py-12 lg:px-8">{query.isLoading ? <div className="h-72 animate-pulse rounded-[28px] bg-[var(--sand)]"/> : query.isError || !trip ? <div className="rounded-[28px] bg-[var(--navy)] p-8 text-white"><h1 className="font-serif text-5xl font-bold">This shared route is unavailable.</h1><Link href="/community" className="mt-6 inline-flex items-center gap-2 rounded-full bg-[var(--gold)] px-5 py-3 text-sm font-extrabold text-[var(--navy)]">Explore the community <ArrowRight size={16}/></Link></div> : <><div className="rounded-[28px] bg-[var(--navy)] p-8 text-white sm:p-12"><div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-[.2em] text-[var(--gold)]"><Globe2 size={14}/>Public route · {trip.destinations[0]?.country ?? "World Trotter"}</div><h1 className="mt-5 max-w-2xl font-serif text-5xl font-bold leading-[.93] tracking-[-.055em] sm:text-6xl">{trip.title}<span className="text-[var(--gold)]">.</span></h1><p className="mt-5 max-w-xl text-sm leading-7 text-white/72">{trip.description || "A shared World Trotter route."}</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={() => copy.mutate()} disabled={copy.isPending} className="inline-flex items-center gap-2 rounded-full bg-[var(--gold)] px-5 py-3 text-sm font-extrabold text-[var(--navy)]"><Copy size={16}/>{copy.isPending ? "Copying…" : "Copy this route"}</button><Link href="/community" className="inline-flex items-center gap-2 rounded-full border border-white/20 px-5 py-3 text-sm font-extrabold text-white">Explore more <ArrowRight size={16}/></Link></div></div><div className="mt-8 space-y-4">{days.map((entry, index) => <ItineraryDayBlock key={entry.day} entry={entry} index={index}/>)}</div></>}</section></AppShell>; }
