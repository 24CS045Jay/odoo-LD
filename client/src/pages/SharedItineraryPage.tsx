// World Trotter visual style: public read-only itinerary that invites a visitor to make it their own.
import { ArrowRight, Copy, Globe2 } from "lucide-react";
import { Link } from "wouter";
import AppShell from "@/components/layout/AppShell";
import ItineraryDayBlock from "@/components/itinerary/ItineraryDayBlock";
import { itineraryDays } from "@/lib/presentationData";
export default function SharedItineraryPage() { return <AppShell><section className="mx-auto max-w-[1000px] px-5 py-12 lg:px-8"><div className="rounded-[28px] bg-[var(--navy)] p-8 text-white sm:p-12"><div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-[.2em] text-[var(--gold)]"><Globe2 size={14}/>Public route · Portugal</div><h1 className="mt-5 max-w-2xl font-serif text-5xl font-bold leading-[.93] tracking-[-.055em] sm:text-6xl">A slower way through the coast<span className="text-[var(--gold)]">.</span></h1><p className="mt-5 max-w-xl text-sm leading-7 text-white/72">A calm, sea-facing itinerary made for people who would rather linger than race through a list.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/register" className="inline-flex items-center gap-2 rounded-full bg-[var(--gold)] px-5 py-3 text-sm font-extrabold text-[var(--navy)]"><Copy size={16}/>Copy this route</Link><Link href="/community" className="inline-flex items-center gap-2 rounded-full border border-white/20 px-5 py-3 text-sm font-extrabold text-white">Explore more <ArrowRight size={16}/></Link></div></div><div className="mt-8 space-y-4">{itineraryDays.map((entry, index) => <ItineraryDayBlock key={entry.day} entry={entry} index={index}/>)}</div></section></AppShell>; }
